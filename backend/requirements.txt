fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# ai_engine's own dependencies -- backend imports ai_engine directly,
# so these need to be installed in the same environment.
httpx>=0.27
scikit-learn>=1.4
langchain-text-splitters>=0.2
langchain-community>=0.2
pypdf>=4.0
python-dotenv>=1.0
