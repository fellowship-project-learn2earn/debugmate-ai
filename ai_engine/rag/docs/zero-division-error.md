# ZeroDivisionError
tags: zerodivisionerror, division by zero

ZeroDivisionError means code divided a number by zero, which is mathematically undefined. This usually happens when a denominator comes from a calculation or user input that can legitimately be zero. Fix by checking the denominator before dividing (if denominator != 0:) or handling the zero case explicitly rather than assuming it won't happen.
