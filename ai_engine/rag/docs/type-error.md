# TypeError
tags: typeerror, unsupported operand, not callable, argument

TypeError means an operation was applied to a value of the wrong type -- e.g. adding a string and an integer, calling something that isn't a function, or passing the wrong number of arguments. Fix by checking the actual type of each value involved with type(x), and converting explicitly (int(), str(), etc.) where a mismatch is found.
